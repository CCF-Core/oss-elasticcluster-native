
bind_address: "{{ ansible_eth1.ipv4.address }}"

consul_ips:
  - 192.168.20.46
  - 8.8.8.8

datacenter: "oss-dev"

# curator_run_frequency in minutes, index_purge_older_than in days
elasticsearch:
  user: elastic
  pass: changeme
  curator_run_frequency: 5
  index_purge_older_than: 5
  cluster_name: "ccf-demo"
  consul_service: "elasticsearch_demo"

remote_user: "vagrant"