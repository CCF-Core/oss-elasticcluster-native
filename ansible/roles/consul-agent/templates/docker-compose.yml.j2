version: '2'
services:
  consul-agent:
    image: ccfoss/consul-agent
    network_mode: host
    ports:
      - "8500:8500"
    environment:
      - "CONSUL={{consul_ips[0]}}:8500"
    command: "consul agent -config-dir /etc/consul.d/ -bind {{ bind_address }}"
    hostname: consul-agent