{
   "datacenter": "{{ datacenter }}",
   "data_dir": "/opt/consul",
   "log_level": "INFO",
   "start_join": ["{{ consul_ips | join(",") }}"]
   "server": false,

   "addresses": {
      "http": "0.0.0.0",
      "dns": "0.0.0.0"
      },

   "ports":  {
      "dns": 53
   }

}
